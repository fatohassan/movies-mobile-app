import React from 'react';
import { View, Text } from "react-native";

function MovieItemView(props) {
    return (
        <View>
            
        </View>
    );
}

export default MovieItemView;
